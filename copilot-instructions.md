# Workspace guidance

- Frontend-only React + Vite prototype; do not add backend services or external APIs.
- Keep analysis, authentication, and history local to the browser.
- Preserve the dark navy, cyan, glassmorphism visual system.
- Validate changes with `npm run build`.
